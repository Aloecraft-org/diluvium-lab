# Plan: numeric core and syntax, September 2026

Three Claude Code sessions, one plan. This document is the coordination
point: each session reads it once at start, works its own section, and
records milestones in its `## Status` block at the bottom of its repo's copy.
The detailed specs it draws on are `diluvium-numeric-spec.md` and
`diluvium-syntax-proposals.md`, committed beside it; this file states what
is decided, in what order, and how the sessions hand off without waiting on
each other.

Commit this file and both specs to `doc/` in diluvium and in diluvium-drt.
The lab session reads them from diluvium.

## 0. Read this first

### 0.1 Spend

Read this plan and the two specs once. Then read only the files your
milestone names. Do not survey the repository, do not spin up exploratory
agents to "understand the codebase", and do not re-derive decisions this
plan already made. Subagents are for exactly two things: running the full
cross-target gate in parallel at a milestone boundary, and one review pass
of a milestone's diff before it merges. Everything else is one agent,
sequential. Measure before you believe a number, but measure once and write
it down. Prefer the existing gates (`examples/run-all.sh`, `expected.txt`,
the diluvium test suite) over new harnesses. When a task is done, say so in
one line in `## Status` with the commit hash, and move on. Under-explaining
is fine; the code and the gate are the record.

### 0.2 Decide, record, proceed

When a question comes up that this plan does not answer, choose the option
most consistent with section 1's principles, write one line under
`## Decisions` in your `## Status` block, and continue. Do not stop to ask.

Stop and wait for the owner only for these:

- A change to `dv.h` beyond what section 3 declares.
- Anything that would make a valid Lua 5.5 program parse or behave
  differently (the freeness test in 3.6 is the check).
- Any change to the behavior of a shipped DRT connector, or any edit under
  the WireGuard, tunnel, relay, TURN, STUN or netcheck code. Those just
  started working in the field; they are not touched this round.
- Any change to how `drt-config` parses a field that exists today. New
  fields are added beside existing ones, never by reworking a shared path,
  and the loader corpus in 3.8 is the check. Every shipped install runs a
  config this loader wrote; a regression here is not caught by any other
  gate.
- A new dependency that adds more than 1 MB to any artifact, or any
  dependency with a non-permissive license.
- Anything that would change the dump/undump format or add an opcode.

### 0.3 Branches and merges

Each session works on one branch (`numeric-syntax` in diluvium, `numeric`
in diluvium-drt, `syntax-highlighting` in lab). Merge to `main` at a
milestone only when the full gate passes on every target CI runs. Never cut
a release or move a tag; leave CHANGELOG entries as `status: unreleased`.
The owner releases.

## 1. What we are building, and the principles that decide ties

Diluvium gains typed arrays, portable numeric kernels, an embedded libm
that makes float transcendentals bit-identical across targets, FFT and NTT,
and a set of syntax additions. DRT gains a `features` compatibility fact,
a raw-buffer lane in the hostcall reply, a parquet/CSV connector, and the
config surface that bounds numeric work per instance. The lab and homepage
gain highlighting for the new syntax and a size ledger for the web profile.

Principles, in the order they win ties:

1. **100% Lua compatibility.** Every new syntax form is a parse error in
   stock Lua 5.5. No new opcodes; desugars emit existing bytecode.
2. **Determinism is reported, not enforced**, except where a program opts
   in (`<deterministic>`). Bit-identical across native, wasm, browser and
   Windows is the reproducible tier's contract.
3. **Core patches minimal**, maintained as the patch series against
   upstream. Everything expressible through `lua.h`/`lauxlib.h` is on-top C.
4. **Feature-gated.** A build without `numeric` is byte-identical to today.
   `slim` stays exactly what it is.
5. **Nothing here needs a new build every four hours.** The shipping DRT
   release path is untouched; this work lands behind a flag and a pin.

Out of scope this round, do not build or explore: GPU backend, yieldable
kernels, the dataframe/stats/ML dlua packages (a later session, see 7),
braces, inline type annotations, Tier D syntax, decQuad (not a dependency
of anything here; arrays are `f64`/`i64`/`u8`).

## 2. Names, with defaults

These defaults are in force unless the owner edits this list before the
sessions start. Sessions do not invent alternatives.

| Thing | Default |
|---|---|
| diluvium build feature | `numeric` (C define `DV_NUMERIC`) |
| array library global and type name | `array` |
| feature query in `dv.h` | `dv_features()` |
| blob adoption in `dv.h` | `dv_array_adopt()` |
| blob accessor in `dv.h` | `dv_reply_blob()` |
| backend vtable in `dv.h` | `dv_numeric_backend` (declared, not populated this round) |
| registry helpers | `__slice`, `dv.defer`, `dv.isa`, `dv.spread` |
| DRT connector | `data`, verbs `read_parquet`, `write_parquet`, `read_csv`, `write_csv` |
| BUILDINFO fields | `features` (per profile), `diluvium_build` |
| instance config block | `numeric = { max_elements = N, max_tier = "exact" \| "reproducible" \| "fast" }` |
| `dv_abi` after this round | `2` |
| NTT prime | `2013265921` (2^31 - 2^27 + 1) |

## 3. Interfaces between sessions

These are fixed so no session waits on another to learn a shape.

### 3.1 `dv.h` additions (diluvium publishes in A0, DRT compiles against)

```c
/* Newline-separated feature names this build carries, e.g. "regex\nnumeric".
   Stable for the life of the process. */
const char *dv_features(void);

/* Monotonic build number, the N in 5.5.1_buildN. */
int dv_build(void);

/* Adopt a host-owned buffer into an array value on the instance's stack.
   dtype: 0=f64 1=i64 2=u8. Takes ownership of `bytes`, which must have
   been allocated with the instance's lua_Alloc. Without DV_NUMERIC this
   pushes a Lua string copy instead and returns 1; returns 0 on adoption. */
int dv_array_adopt(dv_instance *inst, int dtype, size_t len, void *bytes);

/* Backend vtable. Declared this round so the shape is fixed; only the
   portable implementation exists. Every entry carries its tier. */
typedef enum { DV_TIER_EXACT, DV_TIER_REPRODUCIBLE, DV_TIER_FAST } dv_tier;
typedef struct dv_numeric_backend {
  int abi;             /* 1 */
  const char *name;    /* "portable" */
  /* function pointers added as kernels land; each paired with a dv_tier */
} dv_numeric_backend;

/* Per-instance numeric bounds and status. */
void dv_numeric_set_max_elements(dv_instance *inst, uint64_t n); /* 0 = none */
void dv_numeric_set_max_tier(dv_instance *inst, dv_tier t);
int  dv_numeric_touched_fast(dv_instance *inst); /* sticky flag for the audit trail */
```

`dv_abi` moves to 2. Rule for `doc/Release.md`: a DRT reports the highest
ABI it speaks and admits any package requiring less or equal.

### 3.2 Hostcall reply blob lane (DRT publishes in B1, diluvium consumes)

The reply struct in `drt-hostcall` gains `blobs: Vec<Vec<u8>>`. A payload
field that carries a column is `{dtype, len, blob: <index>}`. On the C side
`dv_reply_blob(inst, index, &ptr, &len)` reads it, and the connector's Lua
wrapper calls into `array` to adopt. Bytes never pass through the serde
encoding. If the current encoding turns out to be one where this is awkward
(JSON with no side channel), the side channel is added to the reply struct
and the payload references it by index; do not base64 a column.

### 3.3 Output format for cross-target examples

Numeric examples never print floats with `%g`. They print IEEE bit
patterns: `("%016x"):format(string.unpack("<I8", string.pack("<d", x)))`,
or `array.bits(a)` which does the same per element. This is what makes one
`expected.txt` valid on glibc, musl, wasmtime, Chromium and mingw, whose
CRTs format decimals differently.

### 3.4 Budget charging

Kernels charge the instruction budget by element count, never by time:
one instruction per 64 elements processed, checked at block boundaries, so
`exceeded` fires inside a GEMM and the exceed point is identical on every
target (replay depends on this). Session A implements the charging against
the hook in `src/dv.c`; session B exposes `max_elements` and `max_tier` in
the instance config and attenuates them at spawn like budgets.

### 3.5 Build flags for every TU under `numeric` and the embedded libm

`-ffp-contract=off -fno-fast-math -fexcess-precision=standard -fno-builtin`
on every target, including the `cc`-driven builds in `diluvium-sys` and
`drt-web`, and the mingw cross-build. GCC on aarch64 contracts by default;
this is the single most likely cause of a cross-target mismatch. A
"contraction canary" test computes an `a*b+c` whose fused and unfused
results differ and asserts the unfused bits. Never enable wasm relaxed-simd.
Embedded libm symbols are prefixed (`dv_exp`, `dv_sin`) so the compiler
cannot substitute or constant-fold the platform versions.

### 3.6 Freeness check

CI builds pristine upstream `luac` from the tree the patch series applies
to, feeds it `test/syntax/freeness/*.lua` (one file per new form), and
fails if any file parses. The reverse corpus (the Lua test suite plus the
examples) must produce identical output before and after.

### 3.7 Pin protocol

Session A records each milestone's commit hash in its `## Status`. Session
B moves the `diluvium` pin in `Cargo.lock` to that hash at the start of its
next milestone, runs the full gate, and records the result. Session B never
waits on A: every B milestone lists what it does if the pin it wants is not
there yet. Session C reads A's `## Status` for the syntax that has actually
landed and highlights only that.

### 3.8 Config-loader regression corpus

`drt-config` is shared by everything, and B2 and B3 both add fields to it.
Before either, session B captures every config shape that is deployed or
shipped into `crates/drt-config/tests/corpus/`, verbatim, not retyped:
the `wg.host.lua` that `wg.sh` writes on a customer machine (it contains
`connectors = { time = {} }`, an empty map through the `as_array` path
that rc6 fixed for five fields), `examples/deployment.json`, every
`.host.lua` under `examples/`, and the rendezvous config with `peers = {}`.
The test loads each, asserts it parses to the same structure it parses to
today (snapshot on first run), and runs in the examples gate. Any loader
change that fails a corpus file is wrong regardless of what it enables.

## 4. Session A: diluvium

Repo: diluvium. Branch: `numeric-syntax`. Syntax work is core patches;
numeric work is on-top C under `DV_NUMERIC`.

**A0. Scaffold and interface (first, small).** Add the `numeric` build
feature, off by default in the Makefile and on in the CI matrix. Add the
3.1 declarations to `dv.h` with stubs that behave as documented when the
feature is off. Add the 3.5 flags to every build path, including a check
that `diluvium-sys`'s `build.rs` in diluvium-drt applies them (read it, do
not edit it; record the exact flag line B needs in `## Status`). Add the
contraction canary. Add the 3.6 freeness CI job. Bump `dv_abi` to 2.
Record the hash. This is what unblocks B; do it before anything else.

**A1. Syntax Tier A.** From the proposals doc section 3: compound
assignment (check whether it already landed; if so, skip), default
parameters, `continue`, the `?.` family, numeric separators and `0b`,
`const`, spread, expression-bodied functions, `defer` via `<close>`. One
freeness file per form. Timebox: if a form fights the parser for more than
a few hours, record why and skip it; none of these blocks anything.

**A2. Numeric Stage 0.** From the numeric spec: the `array` userdata,
dtypes `f64`/`i64`/`u8`, views, metamethods, the kernel list, the canonical
reduction order exactly as the spec states it, `group_index` and segmented
reductions, `array.bits`. `dv_array_adopt` becomes real. Budget charging per
3.4. Portable kernels only, all reproducible tier by construction. Tests:
a corpus whose `bits` output is diffed across native x86-64, aarch64 and
wasm32. This is the milestone B waits on; if A1 is running long, finish A2
first.

**A3. Numeric Stage 1.** Vendor the openlibm subset the spec lists, under
`dv_` prefixes, with its license. Route `math.*` through it at init. Extend
the analyzer and the runtime status flag so `math.*` are no longer taint
sources and `dv_numeric_touched_fast` is set by any fast-tier backend (none
exists yet; the flag is wired and tested with a mock). Expect some existing
`expected.txt` files to change in the last digits on some target; that is
the correct outcome and the diff is the record.

**A4. Syntax Tier B.** Lambdas `|x| expr`, `if` and `switch` expressions,
destructuring, slicing via `__slice`, `export`, and `<deterministic>` as a
compile-time attribute: the parser keeps a side list of attributed protos
and runs the analyzer on them at the end of compilation, so nothing new
persists in the chunk. Confirm `switch` is landed before adding its
expression form.

**A5. Numeric Stage 2.** `c128` dtype, radix-2 iterative FFT and real FFT
with twiddles computed directly per index from `dv_sin`/`dv_cos` (no
recurrence), power-of-two sizes only. NTT over the 2.x prime with 64-bit
intermediates (no 128-bit needed at 31 bits), exact convolution and
correlation. Bit-identical across targets is the acceptance.

**A6. Classes.** The minimal form in the proposals doc section 5.1,
including copying parent `__` entries at class creation. `@` is `self`.
Stop here; do not add mixins, properties or `is`.

## 5. Session B: diluvium-drt

Repo: diluvium-drt. Branch: `numeric`. The hole-punch path is not touched.

**B0. Loader corpus, then compatibility facts (no diluvium dependency).**
First, before anything else in this session: the corpus in 3.8, green
against today's loader, committed. Only then: `features:` per
profile in `CHANGELOG.yaml`, `BUILDINFO.txt` and `drt buildinfo`, read from
`dv_features()` once the A0 pin lands and hard-coded from the profile until
then with a `TODO(A0)`. `diluvium_build` beside the revision. The `dv_abi`
rule in `doc/Release.md`. `needs_build` in `examples/meta.json` extended to
features. Start the 0.6.0 entry as `unreleased`, minor by the rule (the
feature set and `full`'s connector list both change).

**B1. Blob lane.** Per 3.2, in `drt-hostcall` and the C glue. Needs A0's
header; until it lands, build against a local copy of the 3.1 declarations
and swap to the pin after. Test: a connector answers a blob and a guest
receives it as a string (the no-`numeric` path), which is fully testable
before arrays exist.

**B2. `data` connector.** `read_parquet` (path, columns, row range),
`write_parquet`, `read_csv` with dtype hints, `write_csv`, scoped to a
granted directory like `fs`. The `parquet` crate with `snappy`, `zstd`,
`lz4` and `gzip` codecs and not `brotli`; record the size delta on `full`.
Decode runs under `spawn_blocking` so the call parks like `rest` does; a
synchronous decode is `exec`'s shape and is wrong here. Nulls per the
spec. `full` only. Example `23-reading-parquet` with `needs_build: full`,
printing `bits`.

**B3. Instance config.** `numeric = { max_elements, max_tier }` in the
config schema, attenuating at spawn under the same rule as budgets (a
child may not raise either; unstated inherits the parent). Wired to
`dv_numeric_set_*`. `dv_numeric_touched_fast` reported by `drt ps` and the
roster and carried on the snapshot beside `exceeded`. Needs the A2 pin for
a real test; before it, the setters are called and the flag is asserted
false.

**B4. Cross-target numeric examples.** Once the A2 pin is in: examples for
arrays, reductions and grouping, later FFT, each printing `bits`, each with
one `expected.txt` diffed on native, wasmtime, Chromium and Windows. The
Windows run is the contraction check; if it diverges, the flags in 3.5 did
not reach a build path, and that is the first thing to look at.

**B5. Notes.** The 0.6.0 changelog entry in the repository's own voice,
`doc/Numeric.md` stating what the tiers promise, and `doc/Failure-Modes.md`
gaining the entry for a kernel that exceeds its budget mid-call.

## 6. Session C: lab and homepage

Repo: diluvium lab (and the site). Branch: `syntax-highlighting`. Small,
mostly following; it must not become a third product.

**C0. Grammar.** PrismJS grammar for every form in the proposals doc,
written from the spec so it can start now, then pruned at each A milestone
to what actually landed. Regex backtick literals and `$"..."` are already
there; check before touching them.

**C1. Size ledger.** Measure `drt_web.tar.gz` and the diluvium wasm module
before this work and after each pin move, gzipped, and keep the numbers in
one table in the lab README. If the `numeric` feature adds more than 500 KB
gzipped to the web module, record it and flag it in `## Status`; do not
change the profile yourself.

**C2. Browser gate.** When B4's examples exist, they run in Chromium
through the existing harness. The REPL transcript diff gains a numeric
line.

**C3. One notebook.** After A5: one notebook that loads a small dataset,
runs a rolling statistic and an FFT, and shows the same `bits` the native
run printed. Not a tutorial series.

## 7. Session D, later: dlua packages

Not started now. Trigger: A2 and B2 merged. Scope: the dataframe and stats
packages from the numeric spec stages 5 and 6, as dollup packages, pulled
from what a real leaf needs rather than built as a library. The owner
starts this one.

## 8. Order of operations and what runs in parallel

```
A0 ──► A1 ──► A2 ──► A3 ──► A4 ──► A5 ──► A6
 │             │      │             │
 ▼             ▼      ▼             ▼
B0 ─► B1 ─► B2 ─► B3 ─► B4 ─────────► B5
C0 ─────────────────► C1 ──► C2 ──► C3
```

A0 is hours and gates B1. A2 gates B3's real test and B4. A5 gates C3.
Everything else proceeds without waiting; each B milestone above says what
it does if its pin is not there.

## 9. Anticipated blockers, pre-answered

- **`setjmp`/`longjmp` on wasm.** Already lowered in the existing wasm
  builds; kernels raising `lua_error` are fine. Do not redesign error
  handling.
- **Two build systems compile the same C.** diluvium's Makefile and
  `diluvium-sys`'s `build.rs`. Flags in 3.5 must reach both; the canary
  test is how you find out they did not.
- **`-fno-builtin` and the libm prefix.** Without both, the compiler will
  replace `exp(x)` with the platform call or fold constants, and the
  cross-target diff will fail on exactly one target for no visible reason.
- **`%g` on Windows.** Never print decimals in examples; 3.3.
- **wasm SIMD.** `-msimd128` is fine; relaxed-simd is not, ever.
- **The instruction hook.** It clears itself in the pcall-escape case
  (FM-4, upstream, known). Kernels charge the budget through the same hook;
  do not try to fix FM-4 here.
- **Lua's raw metamethod lookup.** Inherited `__tostring`/`__add` are not
  found through `__index`; the class desugar copies them. Already in the
  spec; do not rediscover it.
- **`__eq` on mixed operands.** Silently false in Lua; `array.eq` exists
  for this reason, same as decQuad's note.
- **Empty table ambiguity in DRT's config loader.** `{}` is both empty list
  and empty map; rc6 fixed five fields and every shipped `wg.host.lua`
  carries `connectors = { time = {} }`. The `numeric` block and the
  `columns` list in the `data` connector use the same `as_array`-tolerant
  path, added beside it, and the corpus in 3.8 is what proves the existing
  fields still load. This is the one item in the plan that can break
  installs already in the field.
- **`exec` still stalls the loop.** Not this round's problem and not to be
  fixed in passing.
- **Parquet string columns.** Dictionary-encoded per the spec: `i64` codes
  plus a Lua table of uniques. Do not return a table of strings for a
  million-row column.
- **Expected.txt drift from libm.** Some existing examples may change
  output in the last digits on some target after A3. Update the file, note
  the target and the function in the commit message, move on.
- **Windows `full` does not build.** `data` is `full` only; do not try to
  build `full` for Windows to test it.

## 10. Status blocks

Each session appends here in its own repo's copy. One line per milestone
with the commit hash, one line per decision. Other sessions read this at
their milestone boundaries.

```
## Status

What is still open and who can close it — the two specs, the homepage
grammar, and whether this branch merges — is tracked beside this file in
`v0.6.0_session_c_coordination.md`.

### Milestones
- (session) (milestone): (hash) (one line)

### Decisions
- (one line each)
```

---

## Kickoff prompts

**Session A (diluvium):**

> Read `doc/Plan-2026-09.md` once, then `doc/diluvium-numeric-spec.md` and
> `doc/diluvium-syntax-proposals.md` once. You are session A. Work the
> milestones in section 4 in order on branch `numeric-syntax`, following
> section 0 exactly: spend as 0.1 says, decide-record-proceed as 0.2 says,
> merge as 0.3 says. A0 first; it unblocks another session. Record each
> milestone's hash in `## Status`. Do not touch anything listed as out of
> scope in section 1.

**Session B (diluvium-drt):**

> Read `doc/Plan-2026-09.md` once, then the two specs beside it once. You
> are session B. Work section 5 in order on branch `numeric`, following
> section 0 exactly. Nothing under WireGuard, tunnel, relay, TURN, STUN or
> netcheck is touched. Each milestone says what to do if the diluvium pin
> you want is not there yet; do that rather than waiting. Record hashes and
> gate results in `## Status`.

**Session C (lab):**

> Read `doc/Plan-2026-09.md` in the diluvium repo once, then the syntax
> proposals doc once. You are session C. Work section 6 on branch
> `syntax-highlighting`, following section 0. Highlight only forms that
> session A's `## Status` says have landed; C0 may draft from the spec but
> prunes before merging. Keep the size ledger current. This is a small
> session; when the sections are done, stop.

---

# Session C's copy

The body above is verbatim from `diluvium-drt@0eef7c8`, which is where
this plan is maintained along with `diluvium`. It is copied here because
section 10 asks each session to append to its own repository's copy and
the Lab had none; edit it there, not here.

## Status

### Milestones

- C C0: `5d0747b` highlighter learns the 5.5 forms, each behind a compile probe the
  *running kernel* answers. On the build10 pin that lights `optional`,
  `compound` and `secure` and leaves `regex`, `separators` and `binary`
  dark, measured rather than assumed. `npm test` green.
- C C1: `b34f4fe` size ledger in `README.md` with the pre-`numeric` baseline at
  `v5.5.1_build10`, and `scripts/size-ledger.mjs` so the next row is a
  command rather than a memory. Nothing to flag against the 500 KB
  threshold: no build with `numeric` exists yet.
- C C2: not started. Blocked on B4 — there are no cross-target numeric
  examples to run in Chromium.
- C C3: not started. Blocked on A5.

### Decisions

- **Neither spec is committed.** `diluvium-syntax-proposals.md` and
  `diluvium-numeric-spec.md` are in neither repository at
  `diluvium@850e00d` / `diluvium-drt@0eef7c8`, so C0 could not be drafted
  "from the spec". It was drafted instead from the forms section 4 names
  and from the language that has already shipped: `CHANGELOG.md`,
  `doc/Guide.md`, `editors/vscode/syntaxes/dlua.tmLanguage.json` and
  `site/static/assets/prism-diluvium.js`. Forms whose *notation* the plan
  does not spell out — `@` for `self`, `<deterministic>`, lambdas,
  destructuring, `__slice` — are deliberately not drafted: a guessed
  notation yields a probe that either never fires or fires on the wrong
  thing, and both are worse than nothing.
- **The prune is a probe, not a reading of A's `## Status`.** 3.7 says C
  reads A's Status and highlights only that. A's Status is empty (there
  is no `numeric-syntax` branch in diluvium yet), so a prose prune would
  delete the draft outright. Instead each form carries a snippet that
  compiles if and only if the build has it — the discipline the keyword
  list already used for `switch` — and the page asks the kernel it is
  actually running. That is strictly stronger than a Status line, it is
  per-page-load rather than per-commit (this Lab has a runtime dropdown),
  and it needs no edit here when A lands a form.
- **Against the Lab's own tokenizer, not PrismJS.** The Lab vendors no
  Prism; `src/notebook/highlight.js` is what colours a notebook, and
  C0 is written there. The Prism grammar C0 names is
  `site/static/assets/prism-diluvium.js` in the diluvium repository,
  which this session cannot push to. The form names and token names here
  match that grammar's so the two can be reconciled in one commit there.
- **C0's "regex backtick literals are already there" was not true of the
  Lab.** `$"..."` was; backticks were not, and a literal containing an
  apostrophe opened a Lua string that swallowed the rest of the line.
  Fixed, behind the probe — the literal landed in build13 and the pin
  here is build10.
- **The ledger measures what this repository carries**: the vendored
  kernel, the swarm module and the baked single-file page.
  `drt_web.tar.gz` is DRT's artifact; it gets a column when a pin brings
  one.
- **Branch.** The plan says `syntax-highlighting`; this session was
  assigned `claude/syntax-highlighting-s9e4sz` and works there.
